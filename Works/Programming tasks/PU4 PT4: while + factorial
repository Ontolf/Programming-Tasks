#include <stdio.h>

int main()
{
    int f = 0;
    printf("Write number to get a factorial\n");
    
    scanf("%d",&f);
    
    int count = 1, i = 1;
    
    while(i < f){
        
        i++;
        count *= i;
        
    }
      printf("Your factorial is: %d\n", count);
    return 0;
}
